<Page
    x:Class="UwpApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UwpApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <d:Page.DataContext>
        <local:Customer></local:Customer>
    </d:Page.DataContext>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <TextBlock x:Name="textBlock1" HorizontalAlignment="Left" Margin="19,32,0,0" TextWrapping="Wrap" Text="Name:" VerticalAlignment="Top"/>
        <TextBox x:Name="textBox1" 
            HorizontalAlignment="Left" Margin="67,32,0,0" 
            TextWrapping="Wrap" VerticalAlignment="Top" 
            Width="185"
            Text="{Binding Name}"/>
        <TextBox x:Name="textBox3" 
            HorizontalAlignment="Left" 
            TextWrapping="Wrap" VerticalAlignment="Top" 
            Width="185"
            Text="{Binding Age}" Margin="67,69,0,0"/>
        <TextBlock x:Name="ageText" HorizontalAlignment="Left" Margin="24,69,0,0" TextWrapping="Wrap" Text="Age:" VerticalAlignment="Top"/>

    </Grid>
</Page>
